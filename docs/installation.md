(installation)=

# Installation

The package is published on [PyPI](https://pypi.org/project/numpy-flint-arb/) and can be installed with `pip` (or any equivalent):

```bash
pip install numpy-flint-arb
```

Next, see the {ref}`section about usage <usage>` to see how to use it.
